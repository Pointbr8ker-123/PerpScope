# docker/docker-compose.yml
#
# Local development stack.
# Starts the FastAPI backend with all required environment variables.
#
# Usage:
#   docker-compose -f docker/docker-compose.yml up
#
# Prerequisites:
#   Create a .env file in the project root (see .env.example)

version: '3.8'

services:

  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    volumes:
      # Mount source code for live reloading during development
      - ../backend:/app/backend
      - ../src:/app/src
    command: >
      uvicorn backend.main:app
      --host 0.0.0.0
      --port 8000
      --reload
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Price update worker — runs the hourly pipeline
  # Uncomment to run the data pipeline locally in Docker
  # price-worker:
  #   build:
  #     context: ..
  #     dockerfile: docker/Dockerfile
  #   env_file:
  #     - ../.env
  #   command: python src/update_data.py prices
  #   depends_on:
  #     backend:
  #       condition: service_healthy